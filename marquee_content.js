// 跑馬燈配置物件，支援多條訊息輪播
const marqueeConfig = {
    // 訊息陣列，每條訊息可獨立設定文字、顏色、速度和背景
    messages: [
        {
            text: "🌊 歡迎體驗桃花島月光藍海！立即預訂享早鳥優惠，平日連住兩晚9折！詳情請洽詢。🌟",
            color: "#0b2545", // 文字顏色（深藍色，與 --dark 一致）
            speed: 30, // 單條訊息滾動速度（秒），數字越小越快
            background: "#e6f0ff" // 背景顏色（淡藍色）
        },
        {
            text: "🎉 生日當月入住，加贈精緻甜點與飲品，浪漫假期等你來！",
            color: "#1e90ff", // 文字顏色（與 --accent 一致）
            speed: 30, // 滾動速度（秒）
            background: "#e6f0ff" // 背景顏色
        },
        {
            text: "🌅 桃花島月光海奇景，晴朗夜晚最佳觀賞時機，攝影行程報名中！",
            color: "#222", // 文字顏色（深灰色）
            speed: 30, // 滾動速度（秒）
            background: "#e6f0ff" // 背景顏色
        }
    ],
    // 訊息輪播間隔（秒），決定每條訊息顯示多久後切換
    cycleInterval: 10
};

/* 使用說明：
1. 修改訊息內容：
   - 在 `messages` 陣列中新增、刪除或修改物件。
   - 每個物件包含：
     - `text`：跑馬燈顯示的文字（支援表情符號，如 🌊、🌟）。
     - `color`：文字顏色，可用 CSS 顏色值（例如 "#ffffff", "rgb(0,0,0)", "red"）。
     - `speed`：單條訊息從左到右滾動的時間（單位：秒），數字越小滾動越快，建議 20-60 之間。
     - `background`：跑馬燈容器背景顏色，可用 CSS 顏色值（例如 "#e6f0ff", "rgba(0,0,0,0.5)"）。
   - 示例：新增一條訊息
     {
         text: "新訊息內容",
         color: "#ff0000",
         speed: 25,
         background: "#ffffff"
     }

2. 修改輪播間隔：
   - `cycleInterval`：控制每條訊息顯示多久後切換（單位：秒），建議 5-15 秒。
   - 若只有一條訊息，`cycleInterval` 無效，訊息將持續滾動。

3. 注意事項：
   - 確保至少有一條訊息（`messages` 陣列不為空）。
   - 修改後儲存檔案，重新載入網頁即可生效。
   - 若要禁用輪播（僅顯示單條訊息），可將 `messages` 設為單一物件陣列，或將 `cycleInterval` 設為很大值（如 9999）。

4. 進階調整：
   - 若需調整跑馬燈高度或字體大小，需修改 HTML/CSS 中的 `.marquee-container` 和 `.marquee-text` 樣式。
   - 暫停/繼續按鈕已內建，點擊「⏸」暫停動畫，點擊「▶」繼續播放。
*/